Python version used: Python 3.7

import the csv file in your AWS database name the table as 'earthqukae'

In application.py:
1. replace the connection string with your own AWS database connection string.

To run locally, run the following commands:

pip install -r requirements
python application.py